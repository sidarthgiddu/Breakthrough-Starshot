
int transmitPin =1;
int recivePin = 2;


void setup()  {
  pinMode (transmitPin, OUTPUT);
  pinMode (recivePin, INPUT);
  
  }

void loop() {
 
  if (digitalRead (transmitReq) == HIGH) { 
    int begin (transmitPin);
    delay(2000) 
    send
  }
}
